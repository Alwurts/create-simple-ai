import type { Installer } from '../types.js';

export const biomeInstaller: Installer = async (config) => {
  // TODO: Implement Biome installer
  // - Create biome.json configuration
  console.log('Setting up Biome...');
};
